// log-drawer.js — per-row log drawer (Phase C6). Stub for Phase B scaffolding.
function openLogDrawer(/* payload */) {
  // Implemented in Phase C6 (fills #log-drawer, shows #log-scrim, InspectAI deep link).
}
function closeLogDrawer() {
  // Implemented in Phase C6.
}
if (typeof window !== 'undefined') {
  window.openLogDrawer = openLogDrawer;
  window.closeLogDrawer = closeLogDrawer;
}
