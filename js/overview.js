// overview.js — Overview section (Phase E). Stub for Phase B scaffolding.
function initOverview() {
  // Implemented in Phase E (pipeline diagram + brief + caveats callout).
}
if (typeof window !== 'undefined') window.initOverview = initOverview;
